//Crea un array o arreglo unidimensional con un tamaño de 5, 
//asignar los valores numéricos manualmente (los que tu quieras) y mostrarlos por pantalla.

var numeros = [2, 6, 7, 8, 1];

var cadena = numeros.join(", ");

    console.log(cadena);
